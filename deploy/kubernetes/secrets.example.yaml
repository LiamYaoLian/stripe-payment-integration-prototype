# Copy to secrets.yaml (never commit real values). In production prefer External Secrets Operator:
# https://external-secrets.io/
apiVersion: v1
kind: Secret
metadata:
  name: stripe-payment-secrets
type: Opaque
stringData:
  DATABASE_URL: postgresql://stripe:CHANGE_ME@postgres:5432/stripe_payment?sslmode=require
  STRIPE_SECRET_KEY: sk_live_CHANGE_ME
  STRIPE_WEBHOOK_SECRET: whsec_CHANGE_ME
  AUTH_JWT_SECRET: CHANGE_ME_32_BYTES_MIN
  METRICS_API_KEY: CHANGE_ME_METRICS_KEY
