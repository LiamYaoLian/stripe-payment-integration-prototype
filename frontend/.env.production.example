# Production builds (npm run build / Docker web image).
# Leave empty when nginx proxies /api to the backend (docker-compose.prod.yml).
# Set to https://api.example.com only for cross-origin API hosting.
VITE_API_URL=
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...
